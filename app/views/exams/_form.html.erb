<%= form_with(model: exam, local: true) do |form| %>
  <% if exam.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(exam.errors.count, "error") %> prohibited this exam from being saved:</h2>

      <ul>
        <% exam.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :date %>
    <%= form.text_field :date %>
  </div>

  <div class="field">
    <%= form.label :glicose %>
    <%= form.text_field :glicose %>
  </div>

  <div class="field">
    <%= form.label :triglycerides %>
    <%= form.text_field :triglycerides %>
  </div>

  <div class="field">
    <%= form.label :cholesterol %>
    <%= form.text_field :cholesterol %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
