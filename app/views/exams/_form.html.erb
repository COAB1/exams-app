<%= form_with(model: exam, local: true) do |form| %>
  <% if exam.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(exam.errors.count, "error") %> prohibited this exam from being saved:</h2>

      <ul>
        <% exam.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group">
    <%= form.label :date %>
    <%= form.text_field :date, class:"form-control", placeholder:"dd/mm/aaaa" %>
  </div>

  <div class="field form-group">
    <%= form.label :glicose %>
    <%= form.text_field :glicose, class:"form-control", placeholder:"Glicose" %>
  </div>

  <div class="field form-group">
    <%= form.label :triglycerides %>
    <%= form.text_field :triglycerides, class:"form-control", placeholder:"Triglycerides" %>
  </div>

  <div class="field form-group">
    <%= form.label :cholesterol %>
    <%= form.text_field :cholesterol, class:"form-control", placeholder:"Cholesterol" %>
  </div>
<br/>
  <div class="actions">
    <%= form.submit class:"btn btn-secondary" %>
  </div>
<% end %>
